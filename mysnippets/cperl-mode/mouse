# -*- mode: snippet -*-
# name: mouse
# key: mouse
# --
use v5.18;
use warnings;
use diagnostics;
use Carp::Assert;

use DDP;

package $1 {

  use Mouse;

  # properties
  has ${2:var1} => (is => '${3:rw}', isa => '${4:Str}');
  $0

  # methods
  sub dump { p shift; }

  __PACKAGE__->meta->make_immutable();
}